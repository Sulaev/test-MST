import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Градиент фона (из Figma)
  static const Color backgroundGradientStart = Color(0xFFE8EEFF); // Светло-голубой
  static const Color backgroundGradientMid = Color(0xFFD4E4F7);   // Голубой
  static const Color backgroundGradientEnd = Color(0xFFE2F0E8);   // Светло-зелёный

  // Текст
  static const Color primaryText = Color(0xFF1A1A1A);
  static const Color secondaryText = Color(0xFF4A4A4A);
  
  // Акцентные цвета
  static const Color accentBlue = Color(0xFF5BA4C4);    // Голубой для "Relax & Focus"
  static const Color accentPurple = Color(0xFFB388EB);  // Фиолетовый камень
  
  // UI элементы
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color buttonPrimary = Color(0xFF1A1A1A);
  static const Color buttonText = Color(0xFFFFFFFF);
}
